package net;

import net.minecraft.client.settings.GameSettings$Options;
import net.optifine.IOptionControl;

public class uC {
   public static GameSettings$Options a(IOptionControl var0) {
      return var0.getOption();
   }
}
