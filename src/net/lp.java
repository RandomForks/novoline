package net;

import net.shadersmod.client.GuiButtonShaderOption;
import net.shadersmod.client.ShaderOption;

public class lp {
   public static ShaderOption b(GuiButtonShaderOption var0) {
      return var0.getShaderOption();
   }

   public static int a(GuiButtonShaderOption var0) {
      return var0.getButtonWidth();
   }
}
