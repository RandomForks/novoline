package net.minecraft.client.stream;

public enum IStream$AuthFailureReason {
   ERROR,
   INVALID_TOKEN;

   private static final IStream$AuthFailureReason[] $VALUES = new IStream$AuthFailureReason[]{ERROR, INVALID_TOKEN};
}
