package net;

import net.minecraft.block.Block;
import net.minecraft.block.state.pattern.BlockHelper;

public class ajT {
   public static BlockHelper a(Block var0) {
      return BlockHelper.forBlock(var0);
   }
}
