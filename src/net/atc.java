package net;

import java.util.UUID;
import viaversion.viarewind.utils.Utils;
import viaversion.viaversion.api.data.UserConnection;

public class atc {
   public static UUID a(UserConnection var0) {
      return Utils.getUUID(var0);
   }
}
